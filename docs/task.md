## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 将任务状态更新为「待验收」
   - **添加任务摘要**：在任务描述开头添加 `**任务摘要：** <一句话总结>`
   - 补充实现细节（修改文件、技术要点等）
   - 如有修改文件，在技术要点中注明
5. **发送完成通知**：⚠️ **重要！必须执行！**
   - 使用命令：`bash /Users/achen/Priv/task-banner/.claude-notify.sh "标题" "描述"`
   - 标题：简短的任务完成说明（如：✅ XXX 功能完成）
   - 描述：一句话总结完成的内容
6. **生成文件**：⚠️ **重要！必须使用 Write 工具生成文件**
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.json` 文件
     - 格式：单个任务用对象，多个任务用数组
     - 示例（单任务）：
       ```json
       {
         "_id": "任务ID",
         "status": "review",
         "summary": "任务摘要（20-50字）",
         "content": "完整的任务内容，包含实现方案、修改文件、技术要点、验证结果等所有信息"
       }
       ```
     - 示例（多任务）：使用 JSON 数组 `[{...}, {...}]`
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.md` 文件
     - 格式：完整的 Markdown 文档（包含 AI 协作指引 + 任务列表）
     - 必须保留所有 `<!-- task-id: xxx -->` 注释
   - 注意：content 字段需要使用 \n 表示换行，使用 \" 转义引号

### 📝 任务摘要编写规范
- **长度**：20-50 字
- **内容**：用一句话概括完成了什么，解决了什么问题
- **示例**：
  - ✅ "实现了用户登录功能，支持账号密码和第三方登录"
  - ✅ "修复了任务列表排序 bug，优化了性能"
  - ✅ "添加了任务导出功能，支持 Markdown 格式"
  - ❌ "将描述从 xxx 改为 xxx"（太粗暴）
  - ❌ "已在 Service 层实现..."（太技术化）

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务摘要必须简洁明了，便于快速理解任务变更内容

---

## 任务列表
共 3 个任务

### 🟡 中优先级

<!-- task-id: e699b792-b8bc-48a7-9065-8babfd887bc4 -->
#### 1. 任务详情-导入优化

**状态：** 待验收
**优先级：** 中
**创建时间：** 2025/11/06 18:18:29
**更新时间：** 2025/11/07 11:03:45

**任务摘要：** 优化了任务详情抽屉中的导入功能，现在导入内容会添加为评论而不是覆盖描述。

**任务需求：**

现在在任务详情页(drawer)中导入, 更新的还是描述, 需要像任务列表中那样添加到评论才对

---

## 🛠️ AI 解决方案

**请在此处提供详细的实现方案：**

### 实现步骤
1. 分析TaskDetailDrawer组件中的导入逻辑
2. 导入addTaskComment API函数
3. 修改processImportedTasks函数，判断当前任务
4. 将导入内容添加为评论而非更新描述
5. 测试验证功能

### 修改的文件
- `/Users/achen/Priv/task-banner/frontend/src/components/TaskDetailDrawer.vue` - 修改导入逻辑

### 技术要点
- 评论系统API集成：使用addTaskComment函数添加评论
- 条件判断逻辑：判断导入的是否为当前打开的任务
- 状态管理：通过currentTask计算属性获取当前任务信息
- 内容格式化：添加"导入内容："前缀标识导入的评论

### 验证结果
- ✅ TypeScript编译通过，无类型错误
- ✅ 导入内容成功添加为任务评论
- ✅ 非当前任务仍正常更新描述
- ✅ 构建和开发服务器运行正常

**任务摘要：** 优化了任务详情抽屉中的导入功能，现在导入内容会添加为评论而不是覆盖描述。

<!-- task-id: 418828dc-db90-4db8-99c8-e9fd5e0d7dc8 -->
#### 2. 操作优化-收起菜单

**状态：** 待验收
**优先级：** 中
**创建时间：** 2025/11/06 16:32:25
**更新时间：** 2025/11/07 11:03:45

**任务摘要：** 实现了点击项目后自动收起左侧菜单的功能，提升用户操作空间。

**任务需求：**

进入项目后(即点击左侧Projects 的某个 item), 我希望可以自动收起左侧菜单

---

## 🛠️ AI 解决方案

**请在此处提供详细的实现方案：**

### 实现步骤
1. 分析Sidebar组件中的项目链接结构
2. 导入useUIStore和useRouter
3. 将router-link改为div，添加点击处理
4. 实现handleProjectClick函数
5. 导航到项目页面并自动收起菜单

### 修改的文件
- `/Users/achen/Priv/task-banner/frontend/src/components/Sidebar.vue` - 修改项目点击逻辑

### 技术要点
- 全局UI状态管理：使用useUIStore控制侧边栏状态
- 路由导航：使用useRouter进行页面跳转
- 事件处理：添加项目点击事件处理函数
- 用户体验优化：点击项目后自动收起侧边栏

### 验证结果
- ✅ TypeScript编译通过，无类型错误
- ✅ 点击项目项目后左侧菜单自动收起
- ✅ 页面导航功能正常
- ✅ 构建和开发服务器运行正常

**任务摘要：** 实现了点击项目后自动收起左侧菜单的功能，提升用户操作空间。

<!-- task-id: a607e976-14d0-4e7e-98f7-3f6f5a04646f -->
#### 3. Projects更新

**状态：** 待验收
**优先级：** 中
**创建时间：** 2025/11/06 16:27:49
**更新时间：** 2025/11/07 11:03:45

**任务摘要：** 修复了删除项目后左侧菜单栏项目列表不刷新的问题，现在删除后列表会立即更新。

**任务需求：**

删除项目后, 左侧菜单栏的项目列表没有刷新

---

## 🛠️ AI 解决方案

**请在此处提供详细的实现方案：**

### 实现步骤
1. 分析ProjectSettingsDialog组件的删除逻辑
2. 导入useProjectStore替代直接API调用
3. 修改handleDelete函数使用store方法
4. 确保删除后store状态正确更新
5. 验证Sidebar组件自动响应store变化

### 修改的文件
- `/Users/achen/Priv/task-banner/frontend/src/components/project/ProjectSettingsDialog.vue` - 修改删除逻辑

### 技术要点
- 状态管理一致性：使用统一的store管理项目列表
- 组件响应式更新：Sidebar组件自动响应store变化
- API调用优化：通过store方法处理删除操作
- 本地状态同步：删除后立即更新本地项目列表

### 验证结果
- ✅ TypeScript编译通过，无类型错误
- ✅ 删除项目后左侧项目列表立即刷新
- ✅ Sidebar组件正确显示更新后的列表
- ✅ 构建和开发服务器运行正常

**任务摘要：** 修复了删除项目后左侧菜单栏项目列表不刷新的问题，现在删除后列表会立即更新。


> 📅 导出时间：2025/11/07 11:04:23
> 🤖 由 Task-Flow 生成