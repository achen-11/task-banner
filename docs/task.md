# Task-Banner - 任务需求文档

## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 更新任务描述，补充实现细节
   - 如有修改文件，在技术要点中注明
   - 添加相关的参考链接（如果有）
5. **返回格式**：保持 Markdown 格式不变，返回完整的文档内容

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务完成后，可以在任务描述末尾添加实现说明

---

## 任务列表

共 3 个任务

### 🟡 中优先级

<!-- task-id: 1760666642312-8ebeg1rar -->
#### 1. UI-任务详情抽屉

**状态：** 已完成
**优先级：** 中
**标签：** UI
**创建时间：** 2025/10/17 10:04:02
**更新时间：** 2025/10/17 10:45:00

**任务描述：**

- [x] 技术要点, 参考链接, 基本用不上
- [x] 标签用的也不多(基本只使用快速选择提供的那几个标签), 但却足足占了 6 行, 需要调整, 最好只保留快速选择的 items, 然后在后面留一个+的 tag, 点击可以添加自定义标签
- [x] 任务状态和优先级移动到任务描述的上方, 任务描述作为最核心的区域

**实现说明：**

已完成任务详情抽屉的所有优化：

1. **移除不常用字段**：移除了"技术要点"和"参考链接"字段，简化界面
2. **优化标签管理**：
   - 所有预定义标签和自定义标签在同一行显示
   - 预定义标签点击即可选中/取消，选中的标签显示关闭按钮
   - 添加了 + 按钮（虚线边框），点击弹出输入框添加自定义标签
   - 标签选择更加直观和高效
3. **调整布局顺序**：
   - 任务标题 → 任务状态和优先级 → 任务描述
   - 任务描述的文本框行数增加到 8 行，获得更多编辑空间
   - 任务描述作为最核心的内容区域

**技术要点：**

- 修改文件：`src/components/TaskDialog.vue`
- 使用 Element Plus 的 Popover 组件实现 + 按钮的弹出输入框
- 添加了标签选择的动画效果（hover 时缩放）

---

<!-- task-id: 1760668784233-3vz9niys7 -->
#### 2. UI-侧边栏

**状态：** 已完成
**优先级：** 中
**标签：** UI
**创建时间：** 2025/10/17 10:39:44
**更新时间：** 2025/10/17 10:45:00

**任务描述：**

- [x] 侧边栏收起时啥都没有, 干脆就全部收起, 只留这个收起/展开的按钮
- [x] 支持 cmd+b 快捷键展开收起

**实现说明：**

已完成侧边栏的所有优化：

1. **完全收起状态**：
   - 侧边栏收起时宽度变为 0，完全隐藏
   - 展开/收起按钮在收起时固定显示在页面左侧（left: 12px）
   - 展开时按钮显示在侧边栏右侧边缘
   - 主内容区域的 margin-left 从 260px 过渡到 0
2. **快捷键支持**：
   - 添加了全局键盘事件监听
   - Cmd+B (Mac) 或 Ctrl+B (Windows/Linux) 切换侧边栏展开/收起
   - 使用 `event.preventDefault()` 防止浏览器默认行为

**技术要点：**

- 修改文件：
  - `src/components/Sidebar.vue` - 添加快捷键监听和样式优化
  - `src/layouts/MainLayout.vue` - 调整主内容区域的 margin
- 使用 Vue 的 `onMounted` 和 `onUnmounted` 生命周期管理事件监听器
- CSS transition 实现平滑的展开/收起动画

---

<!-- task-id: 1760668847267-fapwrziqz -->
#### 3. UI-列表视图

**状态：** 已完成
**优先级：** 中
**标签：** UI
**创建时间：** 2025/10/17 10:40:47
**更新时间：** 2025/10/17 10:45:00

**任务描述：**

- [x] 已完成和待办任务的区别需要更加明显一些
- [x] Task-ID 其实没必要显示

**实现说明：**

已完成列表视图的所有优化：

1. **隐藏 Task-ID 列**：
   - 移除了表头和表格内容中的 Task-ID 列
   - 调整网格布局从 `40px 120px 1fr 150px 120px 40px` 到 `40px 1fr 150px 120px 40px`
   - 界面更加简洁，聚焦于任务标题和状态
2. **已完成任务视觉区分**：
   - 已完成任务添加了 `task-completed` class
   - 背景色变为浅灰色 (#f5f5f4)
   - 整体透明度降低到 75%
   - 任务标题添加删除线效果
   - 标题颜色变为灰色 (#9ca3af)
3. **状态颜色标识**：
   - 已完成：绿色 (#10b981)，加粗显示
   - 进行中：蓝色 (#3b82f6)
   - 待办：灰色 (#6b7280)
   - 已发送AI：紫色 (#8b5cf6)
   - 需优化：橙色 (#f59e0b)

**技术要点：**

- 修改文件：`src/components/ListView.vue`
- 使用动态 class 绑定实现条件样式
- CSS 渐变和透明度调整增强视觉层次感

---


> 📅 导出时间：2025/10/17 10:40:54
> 🤖 由 Task Banner 生成