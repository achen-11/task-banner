# Task-Banner - 任务需求文档

## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 更新任务描述，补充实现细节
   - 如有修改文件，在技术要点中注明
   - 添加相关的参考链接（如果有）
5. **返回格式**：保持 Markdown 格式不变，返回完整的文档内容

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务完成后，可以在任务描述末尾添加实现说明

---

## 任务列表

共 1 个任务

### 🟡 中优先级

<!-- task-id: 1760665127674-09ptm9jyx -->
#### 1. UI优化

**状态：** 已完成
**优先级：** 中
**标签：** UI
**创建时间：** 2025/10/17 09:38:47
**更新时间：** 2025/10/17 10:05:00

**任务描述：**

- [x] 支持侧边栏收起
- [x] 看板视图, 移除已提交 AI 的状态
- [x] 列表视图
    - [x] 移除头部的 Welcome backxxx, 以及下面的 Here's xxx, 直接是筛选区域即可
    - [x] task item 支持点击直接打开, 而不是 要点更多, 然后再点编辑

**实现细节：**

**1. 侧边栏收起功能**
- 在 `Sidebar.vue` 中添加收起状态 `isCollapsed`
- 添加收起/展开按钮，显示在侧边栏右侧边缘
  - 展开状态：显示双箭头向左图标（«）
  - 收起状态：显示双箭头向右图标（»）
- 收起时侧边栏宽度从 260px 收缩为 60px
- 收起时隐藏所有文字内容，仅显示收起按钮
- 通过 emit 事件通知 `MainLayout.vue` 更新主内容区 margin-left
- 添加 0.3s 过渡动画，实现平滑展开/收起效果

**2. 看板视图优化**
- 移除"已发送AI"状态列（sent_to_ai）
- 看板列数从 5 列改为 4 列（grid-cols-5 → grid-cols-4）
- 保留的状态列：
  - 待办（todo）
  - 进行中（in_progress）
  - 已完成（completed）
  - 需优化（needs_optimization）

**3. 列表视图优化**
- **移除欢迎信息**：
  - 删除 "Welcome back!" 标题
  - 删除 "Here's a list of your tasks for this month!" 副标题
  - 筛选栏直接显示在顶部

- **任务项点击优化**：
  - 整行添加 `@click` 事件，点击任意位置打开编辑对话框
  - 复选框区域添加 `@click.stop` 阻止冒泡
  - 操作菜单区域添加 `@click.stop` 阻止冒泡
  - 行添加 `cursor: pointer` 样式，提示可点击
  - 保留 hover 效果提升用户体验

**修改文件：**
- 修改：`src/components/Sidebar.vue` (添加收起功能和按钮)
- 修改：`src/layouts/MainLayout.vue` (响应侧边栏收起状态)
- 修改：`src/views/Board.vue` (移除已发送AI状态列，改为4列布局)
- 修改：`src/components/ListView.vue` (移除欢迎信息，添加行点击功能)

---


> 📅 导出时间：2025/10/17 10:05:00
> 🤖 由 Task Banner 生成