# Task-Banner - 任务需求文档

## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 更新任务描述，补充实现细节
   - 如有修改文件，在技术要点中注明
   - 添加相关的参考链接（如果有）
5. **返回格式**：保持 Markdown 格式不变，返回完整的文档内容

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务完成后，可以在任务描述末尾添加实现说明

---

## 任务列表

共 3 个任务（已完成 3 个）

### 🟡 中优先级

<!-- task-id: 1760836275454-v38bkire8 -->
#### 1. UI 优化

**状态：** 已完成
**优先级：** 中
**标签：** UI
**创建时间：** 2025/10/20 22:34:45
**更新时间：** 2025/10/20 23:30:00

**任务描述：**

- [x] 回退你在 Project 新增的add icon
- [x] 我指的是侧边栏的 PROJECTS 的 右侧添加这个 add icon

**实现说明：**
1. 移除了 Projects.vue 中"新建项目"和"创建第一个项目"按钮中的 Plus 图标
2. 在 Sidebar.vue 的 PROJECTS 区域标题右侧添加了 add icon 按钮
3. 点击该按钮会跳转到 /projects 页面，用户可以在那里创建新项目
4. 使用 flexbox 布局使标题和按钮在一行两端对齐

**修改文件：**
- `src/views/Projects.vue`
- `src/components/Sidebar.vue`

---

<!-- task-id: 1760972136006-agqwfry04 -->
#### 2. 迭代历史 BUG

**状态：** 已完成
**优先级：** 中
**标签：** Bug
**创建时间：** 2025/10/20 22:55:36
**更新时间：** 2025/10/20 23:30:00

**任务描述：**

- [x] 通过点击导入任务时, 迭代内容会变为"AI 导入.."之类的
- [x] 通过 cmd+i 时则会丢失迭代历史
- [x] 建议: 通过导入更新任务时可以做一个总结(可以在导出提示词中做, 在导入方法中适配), 或者延续正常的保存时的迭代历史的解决方案

**实现说明：**
1. **ImportDialog.vue**: 修改了导入逻辑，优先使用导入内容中的 changelog。如果导入的任务包含 changelog，则完整保留；如果没有，才添加"AI回填"记录
2. **Board.vue (Cmd+I)**: 修改了快捷导入逻辑，优先使用导入内容中的 changelog。如果导入的任务包含 changelog，则使用导入的；如果没有，则保留现有任务的 changelog
3. 这样可以确保当从 AI 导出的任务被重新导入时，完整的迭代历史会被保留

**修改文件：**
- `src/components/ImportDialog.vue` (lines 94-114)
- `src/views/Board.vue` (lines 311-326)

---

<!-- task-id: 1760972187805-w9vmsv5va -->
#### 3. 快捷键冲突

**状态：** 已完成
**优先级：** 中
**标签：** Bug
**创建时间：** 2025/10/20 22:56:27
**更新时间：** 2025/10/20 23:30:00

**任务描述：**

- [x] 全局搜索cmd+k和清空任务表单 cmd+shift+k 会冲突

**实现说明：**
在 GlobalSearch.vue 的快捷键处理函数中添加了 `!event.shiftKey` 检查，确保只有 Cmd+K（不按 Shift）时才触发全局搜索。这样 Cmd+Shift+K 就可以专门用于清空任务表单，不会触发全局搜索。

**修改文件：**
- `src/components/GlobalSearch.vue` (line 165)

**技术要点：**
- 在键盘事件处理中添加 shift 键的判断：`&& !event.shiftKey`
- 确保 Cmd+K 和 Cmd+Shift+K 各自独立工作

---


> 📅 导出时间：2025/10/20 23:35:00
> 🤖 由 Task Banner 生成