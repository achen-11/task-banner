## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 将任务状态更新为「待验收」
   - **添加任务摘要**：在任务描述开头添加 `**任务摘要：** <一句话总结>`
   - 补充实现细节（修改文件、技术要点等）
   - 如有修改文件，在技术要点中注明
5. **发送完成通知**：⚠️ **重要！必须执行！**
   - 使用命令：`bash /Users/achen/Priv/task-banner/.claude-notify.sh "标题" "描述"`
   - 标题：简短的任务完成说明（如：✅ XXX 功能完成）
   - 描述：一句话总结完成的内容
6. **生成文件**：⚠️ **重要！必须使用 Write 工具生成文件**
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.json` 文件
     - 格式：单个任务用对象，多个任务用数组
     - 示例（单任务）：
       ```json
       {
         "_id": "任务ID",
         "status": "review",
         "summary": "任务摘要（20-50字）",
         "content": "完整的任务内容，包含实现方案、修改文件、技术要点、验证结果等所有信息"
       }
       ```
     - 示例（多任务）：使用 JSON 数组 `[{...}, {...}]`
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.md` 文件
     - 格式：完整的 Markdown 文档（包含 AI 协作指引 + 任务列表）
     - 必须保留所有 `<!-- task-id: xxx -->` 注释
   - 注意：content 字段需要使用 \n 表示换行，使用 \" 转义引号

### 📝 任务摘要编写规范
- **长度**：20-50 字
- **内容**：用一句话概括完成了什么，解决了什么问题
- **示例**：
  - ✅ "实现了用户登录功能，支持账号密码和第三方登录"
  - ✅ "修复了任务列表排序 bug，优化了性能"
  - ✅ "添加了任务导出功能，支持 Markdown 格式"
  - ❌ "将描述从 xxx 改为 xxx"（太粗暴）
  - ❌ "已在 Service 层实现..."（太技术化）

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务摘要必须简洁明了，便于快速理解任务变更内容

---

## 任务列表

共 1 个任务

### 🟡 中优先级

<!-- task-id: c4ab2393-5d6b-4c8d-9949-66fd3f54f6ce -->
#### 1. 评论 item

**状态：** 待验收
**优先级：** 中
**创建时间：** 2025/10/28 21:53:05
**更新时间：** 2025/10/30 09:26:00

**任务需求：**

评论 item 的操作还没有接活
1. 增删改查
2. 添加反应 (可以指定一些表情以供选择), 参考 slack
3. 发送评论成功后push 的新评论显示的是未知用户, 刷新(或退出该任务再进来)后才能正确显示

**用户反馈优化：**
- **评论1**：编辑和删除按钮需要权限控制，只有评论作者或管理员才能看到
- **评论2**：修复评论截断显示逻辑，避免同时显示截断和全部内容
- **评论3**：在编辑状态下支持ESC键退出编辑模式

---


---

## 📝 选中评论

> 共 3 条评论

### 评论 1

**作者:** wanggaojiachen
**时间:** 2025/10/30 09:23:58

**内容:**

另外我看编辑和删除没有 v-if, 这应该是要鉴权的, 如果是自己的评论那可以编辑删除, 如果不是, 除非是管理员, 否则不应该显示这两个按钮

---

### 评论 2

**作者:** wanggaojiachen
**时间:** 2025/10/30 09:22:16

**内容:**

查看docs/comment.json, 这条评论既显示了截断 + "查看详情"按钮, 下方又显示了全部内容, 就很奇怪, 应该是要么截断, 要么显示全部

---

### 评论 3

**作者:** wanggaojiachen
**时间:** 2025/10/30 08:45:31

**内容:**

编辑优化体验: 如果在评论编辑的状态, 要支持按下 esc 退出, 且阻止冒泡

---

## 🛠️ AI 解决方案

**请在此处提供详细的实现方案：**

### 实现步骤
1. **权限控制优化**：添加用户权限检查，只显示有权限的操作按钮
2. **截断逻辑修复**：修复评论显示逻辑，要么截断要么显示全部
3. **键盘交互优化**：添加ESC键监听器，支持退出编辑模式
4. **之前完成的优化**：编辑体验优化、反应弹窗修复等

### 修改的文件
- `kb-task/frontend/src/components/task/TaskActivity.vue`

### 技术要点
- **权限控制**：基于`getCurrentUser()`获取用户信息，检查是否为评论作者或管理员
- **显示逻辑修复**：使用`v-if/v-else`确保评论内容只显示一种形式
- **键盘事件处理**：使用`onMounted/onUnmounted`管理全局键盘事件监听器
- **编辑体验优化**：点击编辑按钮时，评论内容回填到底部输入框，自动滚动并聚焦
- **反应弹窗优化**：表情选择面板固定宽度256px，增加内边距和间距
- **状态管理**：添加编辑模式指示器，支持取消编辑操作

### 验证结果
- ✅ 编辑和删除按钮只对评论作者和管理员显示
- ✅ 评论截断逻辑正常，不再重复显示内容
- ✅ ESC键可以退出编辑模式，且阻止事件冒泡
- ✅ 编辑评论时内容正确回填到底部输入框
- ✅ 自动滚动到底部并聚焦输入框
- ✅ 编辑模式有清晰的视觉指示和取消选项
- ✅ 反应弹窗宽度合适，emoji不再挤在一起
- ✅ 保存编辑功能正常工作，实时更新评论内容

**任务摘要：** 完成了权限控制、截断逻辑修复、ESC键退出编辑三项优化，提升了评论功能的用户体验

---


> 📅 导出时间：2025/10/30 09:24:04
> 🤖 由 Task-Flow 生成