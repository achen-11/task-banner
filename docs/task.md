## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 将任务状态更新为「已完成」
   - **添加任务摘要**：在任务描述开头添加 `**任务摘要：** <一句话总结>`
   - 补充实现细节（修改文件、技术要点等）
   - 如有修改文件，在技术要点中注明
5. **发送完成通知**：⚠️ **重要！必须执行！**
   - 使用命令：`bash /Users/achen/Priv/task-banner/.claude-notify.sh "标题" "描述"`
   - 标题：简短的任务完成说明（如：✅ XXX 功能完成）
   - 描述：一句话总结完成的内容
6. **生成文件**：⚠️ **重要！必须使用 Write 工具生成文件**
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.json` 文件
     - 格式：单个任务用对象，多个任务用数组
     - 示例（单任务）：
       ```json
       {
         "_id": "任务ID",
         "status": "completed",
         "summary": "任务摘要（20-50字）",
         "content": "完整的任务内容，包含实现方案、修改文件、技术要点、验证结果等所有信息"
       }
       ```
     - 示例（多任务）：使用 JSON 数组 `[{...}, {...}]`
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.md` 文件
     - 格式：完整的 Markdown 文档（包含 AI 协作指引 + 任务列表）
     - 必须保留所有 `<!-- task-id: xxx -->` 注释
   - 注意：content 字段需要使用 \n 表示换行，使用 \" 转义引号

### 📝 任务摘要编写规范
- **长度**：20-50 字
- **内容**：用一句话概括完成了什么，解决了什么问题
- **示例**：
  - ✅ "实现了用户登录功能，支持账号密码和第三方登录"
  - ✅ "修复了任务列表排序 bug，优化了性能"
  - ✅ "添加了任务导出功能，支持 Markdown 格式"
  - ❌ "将描述从 xxx 改为 xxx"（太粗暴）
  - ❌ "已在 Service 层实现..."（太技术化）

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务摘要必须简洁明了，便于快速理解任务变更内容

---

## 任务列表

共 2 个任务

### 🟡 中优先级

<!-- task-id: 9c486221-2a16-4880-ae0f-aafb69317421 -->
#### 1. Model-Task 调整

**状态：** 已完成
**优先级：** 中
**创建时间：** 2025/10/28 23:52:20
**更新时间：** 2025/10/29 00:03:00

**任务摘要：** 修复了Task模型displayId字段的唯一约束，允许不同项目的任务displayId重复

**任务描述：**

不同项目的 task 的 displayId 是可以重复的, 现在的 model 定义了 unique 导致不能重复

**实现方案：**
- 移除Task模型中displayId的全局unique约束
- 添加复合唯一索引（projectId + displayId），确保同一项目内displayId唯一
- 修改数据库索引结构，支持跨项目的displayId重复

**修改文件：**
- `kb-task/src/code/Models/Task.ts` - 移除全局unique约束，添加复合唯一索引

**技术要点：**
- 使用复合唯一索引 `[projectId, displayId]` 替代全局unique约束
- 保持数据完整性的同时支持业务需求
- 确保同一项目内displayId仍然唯一，避免混乱

---

<!-- task-id: 3bb335fe-59dd-4643-9d60-033892572d8e -->
#### 2. 项目信息编辑

**状态：** 已完成
**优先级：** 中
**创建时间：** 2025/10/28 22:30:13
**更新时间：** 2025/10/29 00:03:00

**任务摘要：** 实现了完整的项目信息编辑功能，包括基础信息编辑、项目删除和自定义图标支持

**任务描述：**

现在项目只有左侧菜单的"➕"可以添加, 没有编辑入口
1. 编辑任务基础信息
2.删除项目(需要二次确认!)
3.任务需要支持 icon 字段的上传

**实现方案：**
- 创建ProjectSettingsDialog组件，提供完整的项目设置界面
- 集成到ProjectView.vue中，通过"项目设置"按钮触发
- 支持项目名称、描述、颜色、图标的编辑
- 实现二次确认的项目删除功能
- 更新前端类型定义，支持icon字段

**修改文件：**
- `frontend/src/components/project/ProjectSettingsDialog.vue` - 新建项目设置对话框组件
- `frontend/src/views/ProjectView.vue` - 集成设置对话框，更新图标显示逻辑
- `frontend/src/types/project.ts` - 更新UpdateProjectParams接口，添加icon字段

**技术要点：**
- 使用ElementPlus Dialog组件构建模态对话框
- 实现表单双向绑定和数据验证
- 双重确认删除机制：输入项目名称 + 确认对话框
- 优先显示自定义图标，回退到项目名称首字母
- 响应式设计，支持实时预览颜色和图标效果


---


> 📅 导出时间：2025/10/29 00:03:00
> 🤖 由 Task-Flow 生成