# Task-FLow - 任务需求文档

## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 更新任务描述，补充实现细节
   - 如有修改文件，在技术要点中注明
   - 添加相关的参考链接（如果有）
5. **返回格式**：保持 Markdown 格式不变，返回完整的文档内容

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务完成后，可以在任务描述末尾添加实现说明

---

## 任务列表

共 1 个任务

### 🟡 中优先级

<!-- task-id: 1761441310933-il3885lfo -->
#### 1. 项目详情 api 接入

**状态：** 已完成
**优先级：** 中
**创建时间：** 2025/10/26 09:15:10
**更新时间：** 2025/10/26 09:15:10

**任务描述：**

- [x] 前端接入项目成员 api 及页面
- [x] 新增tab: "模块"和"标签"管理

**实现细节：**

已完成项目详情页的成员管理功能集成和新 tab 添加：

1. **成员管理页面** (`frontend/src/views/ProjectView.vue:342-396`)
   - 集成后端 `/api/project/members` API
   - 实现成员列表展示：
     - 成员头像（渐变色背景 + 用户名首字母）
     - 成员信息（用户ID、加入时间）
     - 角色徽章（所有者/管理员/成员）
     - 移除成员按钮（不可移除所有者）
   - 空状态提示和"添加成员"按钮
   - 懒加载机制：切换到成员 tab 时才加载数据

2. **新增 Tab 页面** (`frontend/src/views/ProjectView.vue:342-373`)
   - **模块管理 Tab** (第 298-318 行)：
     - 模块列表展示区域
     - "新建模块"按钮
     - 空状态提示：模块可以帮助你更好地组织任务
   - **标签管理 Tab** (第 320-340 行)：
     - 标签列表展示区域
     - "新建标签"按钮
     - 空状态提示：标签可以帮助你快速筛选和分类任务

3. **Tab 导航更新** (`frontend/src/views/ProjectView.vue:342-373`)
   - 更新 tabs 配置，新增：
     - `modules` - 模块管理（网格图标）
     - `tags` - 标签管理（标签图标）
   - Tab 顺序：概览、列表、看板、模块、标签、成员

4. **API 集成** (`frontend/src/views/ProjectView.vue:405-406, 561-576`)
   - 导入 `projectApi` 和 `ProjectMember` 类型
   - 添加 `members` 和 `loadingMembers` 状态
   - 实现 `loadMembers()` 函数调用 API
   - 在 Tab 切换和路由变化时自动加载数据

5. **辅助函数** (`frontend/src/views/ProjectView.vue:524-547`)
   - `getUserInitial()` - 获取用户名首字母
   - `getRoleText()` - 角色文本映射（owner → 所有者）
   - `getRoleBadgeClass()` - 角色徽章样式（不同颜色）

**UI 特性：**

- 成员列表 hover 效果（背景色变化）
- 角色徽章彩色标识（紫色/蓝色/灰色）
- 响应式设计，适配不同屏幕
- 统一的空状态提示和图标
- 优雅的加载状态处理

**技术要点：**

- 懒加载优化：只在需要时加载成员数据
- 类型安全：使用 TypeScript 类型定义
- 组件化设计：可复用的辅助函数
- 用户体验优化：hover、transition 动画

**相关文件：**

- `frontend/src/views/ProjectView.vue` - 项目详情页主组件
- `frontend/src/api/project.ts` - 项目 API 接口
- `frontend/src/types/project.ts` - 类型定义
- `kb-task/src/api/project.ts` - 后端成员 API

---


> 📅 导出时间：2025/10/26 09:15:13
> 🤖 由 Task Banner 生成