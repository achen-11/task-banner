## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 将任务状态更新为「已完成」
   - **添加任务摘要**：在任务描述开头添加 `**任务摘要：** <一句话总结>`
   - 补充实现细节（修改文件、技术要点等）
   - 如有修改文件，在技术要点中注明
5. **发送完成通知**：⚠️ **重要！必须执行！**
   - 使用命令：`bash /Users/achen/Priv/task-banner/.claude-notify.sh "标题" "描述"`
   - 标题：简短的任务完成说明（如：✅ XXX 功能完成）
   - 描述：一句话总结完成的内容
6. **生成文件**：⚠️ **重要！必须使用 Write 工具生成文件**
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.json` 文件
     - 格式：单个任务用对象，多个任务用数组
     - 示例（单任务）：
       ```json
       {
         "_id": "任务ID",
         "status": "completed",
         "summary": "任务摘要（20-50字）",
         "content": "完整的任务内容，包含实现方案、修改文件、技术要点、验证结果等所有信息"
       }
       ```
     - 示例（多任务）：使用 JSON 数组 `[{...}, {...}]`
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.md` 文件
     - 格式：完整的 Markdown 文档（包含 AI 协作指引 + 任务列表）
     - 必须保留所有 `<!-- task-id: xxx -->` 注释
   - 注意：content 字段需要使用 \n 表示换行，使用 \" 转义引号

### 📝 任务摘要编写规范
- **长度**：20-50 字
- **内容**：用一句话概括完成了什么，解决了什么问题
- **示例**：
  - ✅ "实现了用户登录功能，支持账号密码和第三方登录"
  - ✅ "修复了任务列表排序 bug，优化了性能"
  - ✅ "添加了任务导出功能，支持 Markdown 格式"
  - ❌ "将描述从 xxx 改为 xxx"（太粗暴）
  - ❌ "已在 Service 层实现..."（太技术化）

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务摘要必须简洁明了，便于快速理解任务变更内容

---

## 任务列表

共 1 个任务

### 🟡 中优先级

<!-- task-id: 34649158-cb45-4bda-882a-93c78250056b -->
#### 1. 导入优化

**状态：** 已完成
**优先级：** 中
**创建时间：** 2025/10/28 22:00:31
**更新时间：** 2025/10/28 22:15:47

**任务摘要：** 修复导入功能问题，优化确认弹窗显示，确保任务状态正确更新，并建立AI提示词规范。

**任务描述：**

1. 现在导入任务后不会更新任务的 updatedAt 字段, 理论上是要更新的
2. 导入任务确认弹窗显示的是未知任务, 导入时应该根据_id 查询然后回填
3. 任务状态现在不会更新
4. 更新提示词, 要要求 ai 任务完成后 task.json 的状态要是待验收, 而不是已完成

**实现方案：**
- **修复 updatedAt 字段更新**：在 `updateTask` 服务函数中添加 `summary` 字段的更新逻辑
- **优化导入确认弹窗**：根据 `_id` 查询现有任务信息并回填，显示任务对比信息
- **修复任务状态更新**：为有 `_id` 的任务添加状态更新逻辑，不仅添加评论
- **建立AI提示词规范**：创建 AI 提示词指南文档，要求完成后状态为 `review`（待验收）

**修改的文件：**
- `src/code/Services/task.ts` - 修复 summary 字段更新
- `frontend/src/components/project/ProjectTaskList.vue` - 优化导入逻辑和确认弹窗
- `frontend/src/types/task.ts` - 添加AI状态说明注释
- `frontend/src/utils/export.ts` - 添加AI状态说明注释
- `docs/ai-prompt-guidelines.md` - 新建AI提示词指南文档

**技术要点：**
- 后端服务层正确处理 summary 字段更新，确保 updatedAt 时间戳更新
- 前端导入时根据 `_id` 查询现有任务，提供详细对比信息
- 区分新建、更新、评论三种导入操作类型
- 确立 `review` 状态作为AI完成的标准，符合质量管控流程

---


> 📅 导出时间：2025/10/28 22:00:48
> 🤖 由 Task-Flow 生成