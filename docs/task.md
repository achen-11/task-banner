# Task-Banner - 任务需求文档

## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 更新任务描述，补充实现细节
   - 如有修改文件，在技术要点中注明
   - 添加相关的参考链接（如果有）
5. **返回格式**：保持 Markdown 格式不变，返回完整的文档内容

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务完成后，可以在任务描述末尾添加实现说明

---

## 任务列表

共 2 个任务，全部已完成 ✅

### ✅ 已完成任务

<!-- task-id: 1760954264474-00bmhwdfy -->
#### 1. 全局搜索

**状态：** 已完成
**优先级：** 中
**标签：** 功能
**创建时间：** 2025/10/20 17:57:44
**更新时间：** 2025/10/20 23:00:00

**任务描述：**

- [x] 在项目下, 新增一个全局搜索框(支持快捷键打开)
- [x] 支持关键字搜索任务(包括历史迭代内容, 任务内容)
- [x] 搜索结果预览需要高亮显示关键字, 若内容过多适当裁剪, 使用...显示

**✅ 实现说明：**
- 创建了 GlobalSearch 组件，实现全局任务搜索功能
- 支持 Cmd+K (Mac) / Ctrl+K (Windows) 快捷键打开搜索面板
- 搜索范围包括：
  - 任务标题
  - 任务描述
  - 任务变更历史（changelog）
- 搜索结果特性：
  - 智能截断：保留关键字周围内容，前后加 `...`
  - 关键字高亮：使用黄色背景高亮匹配文本
  - 分类显示：标注匹配类型（标题/描述/历史）
  - 上下文预览：显示匹配内容的上下文
- 键盘导航支持：
  - ↑↓ 键切换选项
  - Enter 键选择并跳转
  - ESC 关闭搜索面板
- 视觉设计：
  - 模态遮罩层 + 中央浮动面板
  - 平滑的淡入淡出动画
  - 响应式布局，最多显示 20 个结果
- 相关文件：
  - `src/components/GlobalSearch.vue` (新建)
  - `src/App.vue` (添加全局组件)
  - `src/components/KeyboardShortcutsPanel.vue` (更新快捷键说明)

---

<!-- task-id: 1760971504662-8q3egh81d -->
#### 2. AI 导入优化

**状态：** 已完成
**优先级：** 中
**创建时间：** 2025/10/20 22:45:04
**更新时间：** 2025/10/20 23:00:00

**任务描述：**

- [x] 阅读 task-backup.md, 上次使用这份任务内容进行 cmd+i 进行快速导入时, 误创建了几个任务, 应该是任务序号产生了误导, 优化导入方法, 应该根据 task_id 进行分割或者你有更好的方法也可以

**✅ 实现说明：**
- 问题分析：
  - 旧方法依赖任务序号（`#### N. 标题`）来分割任务
  - task-backup.md 中有些 task-id 注释格式不规范（如 `# <!-- task-id: xxx -->`）
  - 导致解析器将 `#` 误判为新任务开始，产生重复或错误的任务
- 优化方案：
  - 重写了 `importTasksFromMarkdown` 函数
  - 改为基于 `<!-- task-id: xxx -->` 注释来分割任务块
  - 使用正则 `/(?:^|\n)(?:#\s*)?<!--\s*task-id:\s*(\S+)\s*-->/g` 查找所有 task-id
  - 按 task-id 位置分割文本，每个块独立解析
  - 标题匹配更宽松：支持 `#### N. 标题` 和 `#### 标题` 两种格式
  - 保留旧的序号分割方式作为降级方案（当找不到 task-id 时使用）
- 优点：
  - 不再受任务序号影响
  - 支持不规范的 task-id 注释格式
  - 更准确地识别和分割任务
  - 向后兼容旧的导出格式
- 相关文件：
  - `src/utils/export.ts` (重写导入解析逻辑)
  - 新增 `parseSingleTask` 函数用于解析单个任务块
  - 新增 `importTasksFromMarkdownLegacy` 作为降级方案

---


> 📅 导出时间：2025/10/20 23:00:00
> 🤖 由 Task Banner 生成