## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 将任务状态更新为「已完成」
   - **添加任务摘要**：在任务描述开头添加 `**任务摘要：** <一句话总结>`
   - 补充实现细节（修改文件、技术要点等）
   - 如有修改文件，在技术要点中注明
5. **发送完成通知**：⚠️ **重要！必须执行！**
   - 使用命令：`bash /Users/achen/Priv/task-banner/.claude-notify.sh "标题" "描述"`
   - 标题：简短的任务完成说明（如：✅ XXX 功能完成）
   - 描述：一句话总结完成的内容
6. **生成文件**：⚠️ **重要！必须使用 Write 工具生成文件**
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.json` 文件
     - 格式：单个任务用对象，多个任务用数组
     - 示例（单任务）：
       ```json
       {
         "_id": "任务ID",
         "status": "completed",
         "summary": "任务摘要（20-50字）",
         "content": "完整的任务内容，包含实现方案、修改文件、技术要点、验证结果等所有信息"
       }
       ```
     - 示例（多任务）：使用 JSON 数组 `[{...}, {...}]`
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.md` 文件
     - 格式：完整的 Markdown 文档（包含 AI 协作指引 + 任务列表）
     - 必须保留所有 `<!-- task-id: xxx -->` 注释
   - 注意：content 字段需要使用 \n 表示换行，使用 \" 转义引号

### 📝 任务摘要编写规范
- **长度**：20-50 字
- **内容**：用一句话概括完成了什么，解决了什么问题
- **示例**：
  - ✅ "实现了用户登录功能，支持账号密码和第三方登录"
  - ✅ "修复了任务列表排序 bug，优化了性能"
  - ✅ "添加了任务导出功能，支持 Markdown 格式"
  - ❌ "将描述从 xxx 改为 xxx"（太粗暴）
  - ❌ "已在 Service 层实现..."（太技术化）

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务摘要必须简洁明了，便于快速理解任务变更内容

---

## 任务列表

共 1 个任务

### 🟡 中优先级

<!-- task-id: 0becba30-a411-4a65-a543-e4934bb1541a -->
#### 1. 导出优化

**状态：** 已完成
**优先级：** 中
**创建时间：** 2025/10/29 09:36:28
**更新时间：** 2025/10/29 09:53:00

**任务摘要：** 统一了任务导出格式，修复了displayId导致的导入失败问题，保持了选中评论的显示

**任务描述：**

1. 在任务列表导出和在任务详情导出的提示词不统一
2. 任务详情导出的任务 ID 取的的 displayId, 在导入时会查找不到
    解决方案1: 导入支持 displayId 查询;
    解决方案2:导出时要导出_id 而不是displayId

**实现方案：**
- 统一任务列表导出和任务详情导出的提示词格式
- 修复任务详情导出中使用displayId的问题，改为使用_id
- 替换自定义导出函数为标准的exportTaskToMarkdown函数

**修改文件：**
- `frontend/src/components/TaskDetailDrawer.vue` - 统一导出格式，修复displayId问题

**技术要点：**
- 使用标准的exportTaskToMarkdown函数确保导出格式一致性
- 导出时使用_id作为唯一标识符，确保导入时能正确匹配
- 保持displayId仅用于界面显示，不影响数据导入导出
- TypeScript编译通过，无类型错误

**验证结果：**
- 任务列表导出和任务详情导出格式统一
- 导出的任务使用_id，导入时可以正确识别
- 提示词格式一致，包含完整的AI协作指引
- 选中的评论内容正确显示，不会丢失
- TypeScript编译通过，无类型错误

---


> 📅 导出时间：2025/10/29 09:53:00
> 🤖 由 Task-Flow 生成