## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 将任务状态更新为「待验收」
   - **添加任务摘要**：在任务描述开头添加 `**任务摘要：** <一句话总结>`
   - 补充实现细节（修改文件、技术要点等）
   - 如有修改文件，在技术要点中注明
5. **发送完成通知**：⚠️ **重要！必须执行！**
   - 使用命令：`bash /Users/achen/Priv/task-banner/.claude-notify.sh "标题" "描述"`
   - 标题：简短的任务完成说明（如：✅ XXX 功能完成）
   - 描述：一句话总结完成的内容
6. **生成文件**：⚠️ **重要！必须使用 Write 工具生成文件**
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.json` 文件
     - 格式：单个任务用对象，多个任务用数组
     - 示例（单任务）：
       ```json
       {
         "_id": "任务ID",
         "status": "review",
         "summary": "任务摘要（20-50字）",
         "content": "完整的任务内容，包含实现方案、修改文件、技术要点、验证结果等所有信息"
       }
       ```
     - 示例（多任务）：使用 JSON 数组 `[{...}, {...}]`
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.md` 文件
     - 格式：完整的 Markdown 文档（包含 AI 协作指引 + 任务列表）
     - 必须保留所有 `<!-- task-id: xxx -->` 注释
   - 注意：content 字段需要使用 \n 表示换行，使用 \" 转义引号

### 📝 任务摘要编写规范
- **长度**：20-50 字
- **内容**：用一句话概括完成了什么，解决了什么问题
- **示例**：
  - ✅ "实现了用户登录功能，支持账号密码和第三方登录"
  - ✅ "修复了任务列表排序 bug，优化了性能"
  - ✅ "添加了任务导出功能，支持 Markdown 格式"
  - ❌ "将描述从 xxx 改为 xxx"（太粗暴）
  - ❌ "已在 Service 层实现..."（太技术化）

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务摘要必须简洁明了，便于快速理解任务变更内容

---

## 任务列表
共 1 个任务

### 🟡 中优先级

<!-- task-id: b4f9d450-e9fb-4978-96f7-03530e1c8281 -->
#### 1. 文档优化

**状态：** 待验收
**优先级：** 中
**创建时间：** 2025/11/06 18:16:46
**更新时间：** 2025/11/06 18:16:57

**任务摘要：** 修复Markdown解析错误、优化目录为左右收起展开、改进快捷键说明展示、实现专注模式隐藏项目头部

**任务需求：**

1. 正确的 content,却显示markdown 解析错误
2. 目录我要的是左右收起展开(给内容区更多的空间, 而不是上下收起)
3. 快捷键说明展示, 需要找一个更合适的展示方式和展示位置
4. 专注模式时, 头部的项目详情也要收起的, 详见: docs/Images/image.png

---

## 🛠️ AI 解决方案

### 实现步骤
1. **修复Markdown解析错误**：升级marked.js API调用方式，使用marked.use替代marked.setOptions，添加详细错误日志
2. **优化目录收起方式**：将目录改为左右滑动收起，使用悬浮按钮控制，增加过渡动画效果
3. **改进快捷键说明**：使用Popover悬浮提示替代固定面板，右下角显示悬浮按钮，hover显示快捷键列表
4. **实现专注模式隐藏头部**：使用provide/inject机制在组件间传递专注模式状态，隐藏项目头部信息

### 修改的文件
- `frontend/src/components/project/ProjectDocuments.vue` - 修复Markdown解析、优化目录收起方式、改进快捷键说明
- `frontend/src/views/ProjectView.vue` - 添加专注模式状态注入、隐藏项目头部

### 技术要点
- **Markdown渲染优化**：使用marked.use配置渲染器，兼容新版本API，增强错误处理
- **左右收起动画**：使用Vue transition组件实现滑动动画，absolute定位悬浮目录面板
- **Popover提示组件**：使用Element Plus的el-popover组件，hover触发显示快捷键说明
- **provide/inject状态管理**：跨组件传递专注模式状态，实现父子组件通信
- **动态样式控制**：使用v-show和动态class控制头部显示和内容区域高度

### 验证结果
- ✅ Markdown解析错误已修复，支持正常的标题渲染和目录生成
- ✅ 目录改为左右收起展开，给内容区域更多空间，过渡动画流畅
- ✅ 快捷键说明改为悬浮按钮+Popover提示，界面更简洁
- ✅ 专注模式时成功隐藏项目头部，内容区域占满全屏


> 📅 导出时间：2025/11/06 18:18:32
> 🤖 由 Task-Flow 生成