## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 将任务状态更新为「待验收」
   - **添加任务摘要**：在任务描述开头添加 `**任务摘要：** <一句话总结>`
   - 补充实现细节（修改文件、技术要点等）
   - 如有修改文件，在技术要点中注明
5. **发送完成通知**：⚠️ **重要！必须执行！**
   - 使用命令：`bash /Users/achen/Priv/task-banner/.claude-notify.sh "标题" "描述"`
   - 标题：简短的任务完成说明（如：✅ XXX 功能完成）
   - 描述：一句话总结完成的内容
6. **生成文件**：⚠️ **重要！必须使用 Write 工具生成文件**
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.json` 文件
     - 格式：单个任务用对象，多个任务用数组
     - 示例（单任务）：
       ```json
       {
         "_id": "任务ID",
         "status": "review",
         "summary": "任务摘要（20-50字）",
         "content": "完整的任务内容，包含实现方案、修改文件、技术要点、验证结果等所有信息"
       }
       ```
     - 示例（多任务）：使用 JSON 数组 `[{...}, {...}]`
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.md` 文件
     - 格式：完整的 Markdown 文档（包含 AI 协作指引 + 任务列表）
     - 必须保留所有 `<!-- task-id: xxx -->` 注释
   - 注意：content 字段需要使用 \n 表示换行，使用 \" 转义引号

### 📝 任务摘要编写规范
- **长度**：20-50 字
- **内容**：用一句话概括完成了什么，解决了什么问题
- **示例**：
  - ✅ "实现了用户登录功能，支持账号密码和第三方登录"
  - ✅ "修复了任务列表排序 bug，优化了性能"
  - ✅ "添加了任务导出功能，支持 Markdown 格式"
  - ❌ "将描述从 xxx 改为 xxx"（太粗暴）
  - ❌ "已在 Service 层实现..."（太技术化）

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务摘要必须简洁明了，便于快速理解任务变更内容

---

## 任务列表
共 1 个任务

### 🟡 中优先级

<!-- task-id: 6d746689-9458-44a9-8e98-7fd4e27a634d -->
#### 1. 看板视图-功能增强

**状态：** 待验收
**优先级：** 中
**创建时间：** 2025/11/14 10:15:59
**更新时间：** 2025/11/14 10:15:59

**任务摘要：** 修复看板视图快捷键，实现与列表视图一致的导入导出功能

**任务需求：**

1. 参照列表视图(或者是复用), 支持"N"新建任务, 移除头部的栏目管理, 支持任务导入导出
2. 参照文档视图, 支持"专注模式", 同时也要支持快捷键打开关闭

---

**实现方案：**

### 实现步骤
1. 参照列表视图，实现"N"快捷键新建任务功能
2. 移除看板视图头部的栏目管理按钮
3. 实现任务导入导出功能，支持JSON格式
4. 参照文档视图，实现专注模式功能
5. 添加F1快捷键支持专注模式切换
6. 完善新建任务功能，实现与列表视图一致的任务创建体验
7. 完善专注模式实现，使其能够隐藏上方的项目信息
8. 修复快捷键注册方式，使Cmd+I和Cmd+E能够正常工作

### 修改的文件
- `/frontend/src/components/project/ProjectBoard.vue`
- `/frontend/src/views/ProjectView.vue`

### 技术要点
- 使用Element Plus的ElMessageBox实现任务导入对话框
- 使用Blob和URL.createObjectURL实现任务导出下载
- 使用v-show指令控制专注模式下头部区域的显示/隐藏
- 使用CSS类和动态样式实现专注模式的视觉效果
- 注册F1快捷键实现专注模式切换
- 在ProjectView中添加对看板视图的专注模式支持
- 实现与列表视图一致的任务创建流程，包括drawerMode状态管理
- 修复快捷键注册方式，使用meta: true而不是同时使用ctrl和meta

### 验证结果
- "N"快捷键新建任务功能正常工作，可以打开任务创建抽屉
- 栏目管理按钮已移除
- 任务导入导出功能正常工作，支持JSON格式
- 专注模式功能正常，可以隐藏头部区域和项目信息
- F1快捷键可以正常切换专注模式
- 新建任务后，任务会正确添加到看板对应列的顶部
- Cmd+I和Cmd+E快捷键现在可以正常工作，与列表视图保持一致

---

## 🛠️ AI 解决方案

**请在此处提供详细的实现方案：**

### 实现步骤
1. 分析任务需求
2. 设计技术方案
3. 具体实现步骤
4. 验证和测试

### 修改的文件
- 文件路径1
- 文件路径2

### 技术要点
- 关键技术1
- 关键技术2

### 验证结果
- 测试结果1
- 测试结果2

**任务摘要：** <请在此处填写20-50字的任务摘要>


> 📅 导出时间：2025/11/14 10:16:15
> 🤖 由 Task-Flow 生成