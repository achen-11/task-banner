# Task-Banner - 任务需求文档

## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 更新任务描述，补充实现细节
   - 如有修改文件，在技术要点中注明
   - 添加相关的参考链接（如果有）
5. **返回格式**：保持 Markdown 格式不变，返回完整的文档内容

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务完成后，可以在任务描述末尾添加实现说明

---

## 任务列表

共 1 个任务

### 🟡 中优先级

<!-- task-id: 1760666068907-0sw76uj5u -->
#### 1. 任务端口切换

**状态：** 已完成
**优先级：** 中
**标签：** 功能
**创建时间：** 2025/10/17 09:54:28
**更新时间：** 2025/10/17 10:24:00

**任务描述：**

- [x] 项目运行端口要切换到 3090
- [x] 我手动这么做时, 数据会丢失, 你要保证在切换成功的同时, 把数据也带上

**实现说明：**

任务已完成。实现了以下功能：

1. **端口配置**：在 `vite.config.ts` 中配置了端口 3090
2. **数据管理页面**：创建了 `/data-management` 页面，提供完整的数据导出导入功能
3. **导出功能**：
   - 导出所有数据（项目、任务、模板、历史记录）为 JSON 格式
   - 自动生成带时间戳的备份文件名
4. **导入功能**：
   - 支持从 JSON 备份文件恢复数据
   - 导入前会提示用户确认（防止误操作）
   - 自动清空旧数据并加载新数据
5. **数据清空功能**：提供了清空所有数据的功能（需二次确认）
6. **数据统计**：实时显示项目数、任务数、模板数、历史记录数

**技术要点：**

- 修改文件：
  - `vite.config.ts` - 配置端口 3090
  - `src/views/DataManagement.vue` - 新建数据管理页面
  - `src/router/index.ts` - 添加数据管理路由
  - `src/components/Sidebar.vue` - 添加导航链接
- 使用 Dexie.js IndexedDB 进行数据持久化
- 使用 Pinia store 进行状态管理
- 数据导出导入通过 `src/db/index.ts` 中的 `exportAllData` 和 `importAllData` 函数实现

**参考资料：**

- [Dexie.js 文档](https://dexie.org/)
- [Vite 配置文档](https://vitejs.dev/config/)

---


> 📅 导出时间：2025/10/17 09:57:13
> 🤖 由 Task Banner 生成