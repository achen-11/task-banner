## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 将任务状态更新为「待验收」
   - **添加任务摘要**：在任务描述开头添加 `**任务摘要：** <一句话总结>`
   - 补充实现细节（修改文件、技术要点等）
   - 如有修改文件，在技术要点中注明
5. **发送完成通知**：⚠️ **重要！必须执行！**
   - 使用命令：`bash /Users/achen/Priv/task-banner/.claude-notify.sh "标题" "描述"`
   - 标题：简短的任务完成说明（如：✅ XXX 功能完成）
   - 描述：一句话总结完成的内容
6. **生成文件**：⚠️ **重要！必须使用 Write 工具生成文件**
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.json` 文件
     - 格式：单个任务用对象，多个任务用数组
     - 示例（单任务）：
       ```json
       {
         "_id": "任务ID",
         "status": "review",
         "summary": "任务摘要（20-50字）",
         "content": "完整的任务内容，包含实现方案、修改文件、技术要点、验证结果等所有信息"
       }
       ```
     - 示例（多任务）：使用 JSON 数组 `[{...}, {...}]`
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.md` 文件
     - 格式：完整的 Markdown 文档（包含 AI 协作指引 + 任务列表）
     - 必须保留所有 `<!-- task-id: xxx -->` 注释
   - 注意：content 字段需要使用 \n 表示换行，使用 \" 转义引号

### 📝 任务摘要编写规范
- **长度**：20-50 字
- **内容**：用一句话概括完成了什么，解决了什么问题
- **示例**：
  - ✅ "实现了用户登录功能，支持账号密码和第三方登录"
  - ✅ "修复了任务列表排序 bug，优化了性能"
  - ✅ "添加了任务导出功能，支持 Markdown 格式"
  - ❌ "将描述从 xxx 改为 xxx"（太粗暴）
  - ❌ "已在 Service 层实现..."（太技术化）

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务摘要必须简洁明了，便于快速理解任务变更内容

---

## 任务列表

共 2 个任务

### 🟡 中优先级

<!-- task-id: 15d83955-3985-48f0-b7f1-b2f0ceed065b -->
#### 1. 导出方法优化

**状态：** 已完成
**优先级：** 中
**创建时间：** 2025/10/29 09:51:35
**更新时间：** 2025/10/29 10:02:00

**任务摘要：** 抽离了重复的AI提示词代码，减少了代码量和维护工作

**任务描述：**

"kb-task/frontend/src/utils/export.ts"的"exportTaskToMarkdown"
和"exportTasksToMarkdown"有重复的提示词, 这应该抽离出来, 减少代码量和维护工作

**实现方案：**
- 创建generateAIGuidelines()函数抽离重复的AI提示词
- 更新两个导出函数使用统一的提示词生成函数
- 删除重复的代码，提高代码可维护性

**修改文件：**
- `frontend/src/utils/export.ts` - 抽离重复代码，创建统一的AI指引生成函数

**技术要点：**
- 将AI协作指引内容抽离到独立函数
- 使用函数调用替代重复代码
- 保持所有导出格式的统一性
- 进一步抽离AI解决方案模板，消除所有重复代码
- TypeScript编译通过，无类型错误

---

<!-- task-id: 29e86b34-e119-453d-b809-167d6fc51386 -->
#### 2. 描述输入框优化

**状态：** 已完成
**优先级：** 中
**创建时间：** 2025/10/29 09:37:38
**更新时间：** 2025/10/29 10:02:00

**任务摘要：** 实现了描述输入框支持Tab键缩进功能，提升了编辑体验

**任务描述：**

1. 需要支持 tab 键缩进(四个空格)

**实现方案：**
- 在MarkdownEditor组件中添加Tab键和Shift+Tab键处理
- 实现单行和多选文本的缩进/反向缩进功能
- 添加工具栏提示，告知用户缩进快捷键

**修改文件：**
- `frontend/src/components/common/MarkdownEditor.vue` - 添加Tab键缩进功能和UI提示

**技术要点：**
- 支持Tab键插入四个空格缩进
- 支持Shift+Tab反向缩进
- 支持多选文本批量缩进处理
- 正确处理光标位置和选中区域
- TypeScript编译通过，无类型错误

---


> 📅 导出时间：2025/10/29 10:02:00
> 🤖 由 Task-Flow 生成