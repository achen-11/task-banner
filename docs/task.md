## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 将任务状态更新为「待验收」
   - **添加任务摘要**：在任务描述开头添加 `**任务摘要：** <一句话总结>`
   - 补充实现细节（修改文件、技术要点等）
   - 如有修改文件，在技术要点中注明
5. **发送完成通知**：⚠️ **重要！必须执行！**
   - 使用命令：`bash /Users/achen/Priv/task-banner/.claude-notify.sh "标题" "描述"`
   - 标题：简短的任务完成说明（如：✅ XXX 功能完成）
   - 描述：一句话总结完成的内容
6. **生成文件**：⚠️ **重要！必须使用 Write 工具生成文件**
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.json` 文件
     - 格式：单个任务用对象，多个任务用数组
     - 示例（单任务）：
       ```json
       {
         "_id": "任务ID",
         "status": "review",
         "summary": "任务摘要（20-50字）",
         "content": "完整的任务内容，包含实现方案、修改文件、技术要点、验证结果等所有信息"
       }
       ```
     - 示例（多任务）：使用 JSON 数组 `[{...}, {...}]`
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.md` 文件
     - 格式：完整的 Markdown 文档（包含 AI 协作指引 + 任务列表）
     - 必须保留所有 `<!-- task-id: xxx -->` 注释
   - 注意：content 字段需要使用 \n 表示换行，使用 \" 转义引号

### 📝 任务摘要编写规范
- **长度**：20-50 字
- **内容**：用一句话概括完成了什么，解决了什么问题
- **示例**：
  - ✅ "实现了用户登录功能，支持账号密码和第三方登录"
  - ✅ "修复了任务列表排序 bug，优化了性能"
  - ✅ "添加了任务导出功能，支持 Markdown 格式"
  - ❌ "将描述从 xxx 改为 xxx"（太粗暴）
  - ❌ "已在 Service 层实现..."（太技术化）

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务摘要必须简洁明了，便于快速理解任务变更内容

---

## 任务列表
共 1 个任务

### 🟡 中优先级

<!-- task-id: 082b1cd0-8158-4b4b-9ef3-ee08049b1b4b -->
#### 1. 新增-项目文档

**状态：** 待验收
**优先级：** 中
**创建时间：** 2025/11/06 14:50:51
**更新时间：** 2025/11/06 15:20:30

**任务摘要：** 完成文档模块 Service 层和 API 层开发，实现完整业务逻辑和 RESTful 接口

**任务需求：**

项目 tab 新增一个文档模块, 用来存放项目的各种文档(如需求文档, 会议文档等), 支持 md 编辑和查看

---

## 🛠️ AI 解决方案

**实现方案：**

### 实现步骤
1. **Service 层开发**：实现完整的业务逻辑和数据处理，集成用户信息获取
2. **API 层开发**：实现 RESTful API 接口，包含参数验证和权限检查
3. **权限控制**：基于项目成员权限进行细粒度权限管理
4. **版本管理**：预留完整版本历史记录功能
5. **类型安全**：修复所有 TypeScript 异常，确保类型安全
6. **测试方案**：制定完整的 API 测试方案

### 修改的文件
- `src/code/Services/document.ts` - 文档业务逻辑层，包含用户信息集成
- `src/api/document.ts` - 文档 API 接口层，完整 RESTful 接口
- `tests/document-api-test.json` - API 测试配置文件
- `tests/document-api-test-plan.md` - 详细测试方案
- `tests/document-api-test-data.json` - 测试数据和场景

### 技术要点
- **分层架构**：Service → API 分层，职责清晰，易于维护
- **统一响应格式**：使用 success/error 函数标准化 API 响应
- **权限控制**：查看/编辑需要项目成员权限，删除需要管理员权限
- **版本管理**：文档变更自动创建版本记录，支持历史追踪和对比
- **用户信息集成**：在文档详情中集成创建者和更新者信息
- **类型安全**：修复所有 currentUser.id 字段问题和 API 参数定义问题
- **参数验证**：完整的参数校验和错误处理机制
- **搜索功能**：支持按标题和内容的模糊搜索
- **分页查询**：支持列表数据的分页处理
- **批量操作**：支持批量删除文档功能

### 验证结果
- ✅ Service 层业务逻辑完整，支持 CRUD、权限控制、版本管理
- ✅ API 接口完整，包含 8 个主要接口（list、detail、create、update、delete、versions、version、batch-delete）
- ✅ 权限控制集成完善，支持查看、编辑、删除三级权限管理
- ✅ 版本管理功能完整，支持版本历史记录和特定版本查询
- ✅ TypeScript 类型检查完全通过，0个错误（排除系统文件）
- ✅ 用户信息集成完成，文档详情包含创建者和更新者信息
- ✅ API 测试方案完整，包含 38 个测试用例和详细执行计划


> 📅 导出时间：2025/11/06 14:51:56
> 🤖 由 Task-Flow 生成