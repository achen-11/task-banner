## 🤖 AI 协作指引

### 任务处理流程
1. **阅读任务**：仔细阅读下方的任务需求，理解每个任务的目标、技术要点和参考资料
2. **实现任务**：根据项目技术栈和任务描述完成开发
3. **保留元数据**：在返回结果时，务必保留每个任务的 task-id 注释（`<!-- task-id: xxx -->`）
4. **更新任务信息**：
   - 将任务状态更新为「待验收」
   - **添加任务摘要**：在任务描述开头添加 `**任务摘要：** <一句话总结>`
   - 补充实现细节（修改文件、技术要点等）
   - 如有修改文件，在技术要点中注明
5. **发送完成通知**：⚠️ **重要！必须执行！**
   - 使用命令：`bash /Users/achen/Priv/task-banner/.claude-notify.sh "标题" "描述"`
   - 标题：简短的任务完成说明（如：✅ XXX 功能完成）
   - 描述：一句话总结完成的内容
6. **生成文件**：⚠️ **重要！必须使用 Write 工具生成文件**
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.json` 文件
     - 格式：单个任务用对象，多个任务用数组
     - 示例（单任务）：
       ```json
       {
         "_id": "任务ID",
         "status": "review",
         "summary": "任务摘要（20-50字）",
         "content": "完整的任务内容，包含实现方案、修改文件、技术要点、验证结果等所有信息"
       }
       ```
     - 示例（多任务）：使用 JSON 数组 `[{...}, {...}]`
   - 使用 Write 工具生成 `/Users/achen/Priv/task-banner/docs/task.md` 文件
     - 格式：完整的 Markdown 文档（包含 AI 协作指引 + 任务列表）
     - 必须保留所有 `<!-- task-id: xxx -->` 注释
   - 注意：content 字段需要使用 \n 表示换行，使用 \" 转义引号

### 📝 任务摘要编写规范
- **长度**：20-50 字
- **内容**：用一句话概括完成了什么，解决了什么问题
- **示例**：
  - ✅ "实现了用户登录功能，支持账号密码和第三方登录"
  - ✅ "修复了任务列表排序 bug，优化了性能"
  - ✅ "添加了任务导出功能，支持 Markdown 格式"
  - ❌ "将描述从 xxx 改为 xxx"（太粗暴）
  - ❌ "已在 Service 层实现..."（太技术化）

### ⚠️ 重要提醒
- 必须保留所有 `<!-- task-id: xxx -->` 注释，这是任务回填的关键标识
- 保持 Markdown 结构完整，不要删除任何标题层级
- 任务摘要必须简洁明了，便于快速理解任务变更内容

---

## 任务列表
共 1 个任务

### 🟡 中优先级

<!-- task-id: f370ae68-aef2-4ea9-b9d2-17a76b4ea163 -->
#### 1. 项目列表

**状态：** 待验收
**优先级：** 中
**创建时间：** 2025/11/20 17:12:47
**更新时间：** 2025/11/20 17:12:47

**任务摘要：** 新增项目列表页并支持项目归档，归档项目从左侧菜单隐藏但可在列表中查看与恢复

**任务需求：**

1. 需要新增一个项目列表页
2. 项目需要支持归档, 归档后不会出现在左侧菜单栏

---

## 🛠️ AI 解决方案

**请在此处提供详细的实现方案：**

### 实现步骤
1. 分析现有项目模型、服务与前端 Store / 路由结构，确定可复用能力
2. 在后端扩展项目更新逻辑，支持修改 `status` 字段为 `archived`
3. 在前端类型与 Store 中加入 `archived` 状态支持
4. 新增 `Projects` 视图与 `/projects` 路由，展示项目列表并提供归档/恢复操作
5. 修改侧边栏项目列表，仅展示未归档项目
6. 在项目详情视图中补充对 `archived` 状态的展示样式

### 修改的文件
- `/src/code/Services/project.ts`：`updateProject` 支持更新 `status`
- `/src/api/project.ts`：项目更新接口透传 `status` 字段
- `/frontend/src/types/project.ts`：`Project` 与 `UpdateProjectParams` 增加 `archived` 状态
- `/frontend/src/stores/project.ts`：增加 `archivedProjects` 计算属性
- `/frontend/src/components/Sidebar.vue`：侧边栏项目列表过滤掉 `archived` 项目
- `/frontend/src/views/ProjectView.vue`：支持展示 `archived` 状态文案与样式
- `/frontend/src/views/Projects.vue`：新增项目列表页与归档/恢复操作
- `/frontend/src/router/index.ts`：新增 `/projects` 路由配置

### 技术要点
- 利用现有 `status` 字段扩展项目生命周期状态，新增 `archived` 状态而无需改动数据结构
- 在 Service 层集中处理项目更新逻辑，保证 API 与前端调用保持简单统一
- 前端通过 Pinia Store 维护项目列表状态，在归档/恢复后自动更新 UI
- 项目列表页支持按状态筛选（全部/进行中/已完成/已暂停/已归档），并提供统一的归档/恢复入口
- 左侧菜单通过本地计算属性过滤 `archived` 项目，避免修改通用接口行为

### 验证结果
- `/projects` 页面可以正常加载项目列表，并按状态筛选
- 点击“归档项目”后，对应项目 `status` 变为 `archived`，从左侧菜单隐藏，但仍在项目列表中显示
- 对已归档项目点击“恢复项目”后，`status` 变为 `active`，重新出现在左侧菜单
- 项目详情页在状态为 `archived` 时，正确显示“已归档”标签与灰色样式
- 所有修改通过前端 Lint 检查


> 📅 导出时间：2025/11/21 10:18:14
> 🤖 由 Task-Flow 生成