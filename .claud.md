# TaskFlow 项目开发偏好

> 这是 Claude Code 的项目偏好配置文件，定义了开发规范和代码风格。

---

## 📋 项目结构

```
task-banner/
├── kb-task/                    # 后端项目（Kooboo）
│   ├── src/
│   │   ├── api/                # API 端点
│   │   ├── code/
│   │   │   ├── Models/         # 数据模型
│   │   │   ├── Services/       # 业务逻辑
│   │   │   └── Utils/          # 工具函数
│   │   └── db/                 # 数据库
│   └── frontend/               # 前端项目（Vue 3）
│       └── src/
│           ├── components/
│           ├── layouts/
│           └── views/
├── docs/                       # 文档
└── .claud.md                   # 本文件
```

---

## 🎯 后端 API 开发规范

### 1. 路由定义

**✅ 必须遵守：**
- 使用 `@k-url /api/resource/{action}` 格式定义路由
- `{action}` 用于动态匹配子路由（如 list, detail, create, update, delete）
- **避免使用 `{id}` 路径参数**，改用 query 或 body 传递

**示例：**
```typescript
// ✅ 推荐
// @k-url /api/project/{action}
// GET /api/project/detail?id=1
k.api.get("detail", () => {
  const id = parseInt(k.request.queryString?.id)
})

// ❌ 避免
// @k-url /api/project/{id}
// GET /api/project/1
```

### 2. 参数传递

| 参数类型 | 传递方式 | 使用场景 |
|---------|---------|---------|
| ID、过滤条件 | Query 参数 | GET 请求 |
| 创建/更新数据 | Body | POST/PUT/DELETE |
| 分页参数 | Query 参数 | GET 列表 |

### 3. 统一响应格式

**只使用两个响应函数：**

```typescript
import { success, error } from 'code/Utils/response'

// 成功响应
return success(data, message)

// 错误响应
return error(message, code, err)
```

**响应格式：**
```typescript
{
  code: number,    // 200 成功，400+ 错误
  message: string, // 消息
  data: any        // 数据（成功时）或 null（失败时）
}
```

### 4. 标准 API 流程

```typescript
k.api.post("create", (body: any) => {
  try {
    // 1. 鉴权检查
    if (!k.account.isLogin) {
      return error('Unauthorized', 401)
    }

    // 2. 获取当前用户
    const currentUser = getUserInfo(k.account.user.current.userName)

    // 3. 参数验证
    if (!body.name) {
      return error('Name is required', 400)
    }

    // 4. 业务逻辑（调用 Service 层）
    const result = createResource(body, currentUser.id)

    // 5. 返回成功
    return success(result, 'Created successfully')

  } catch (err) {
    // 6. 错误处理
    return error('Failed to create', 500, err)
  }
})
```

### 5. 数据库操作

**优先使用 K_SQLITE_ORM：**
```typescript
// ✅ 推荐
import { Project } from 'code/Models/project'

const project = Project.findById(id)
const projects = Project.findAll({ userId })
Project.create({ name, description })
Project.updateById(id, { name })

// ❌ 避免（除非 ORM 无法满足）
k.DB.sqlite.query('SELECT * FROM projects WHERE id = @id', { id })
```

---

## 🎨 前端开发规范

### 1. Vue 3 Composition API

- 使用 `<script setup lang="ts">` 语法
- 优先使用 Composition API（ref, reactive, computed, watch 等）
- TypeScript 类型定义

### 2. Tailwind CSS

- 使用 Tailwind CSS 类名，避免自定义 CSS
- 遵循设计系统的颜色、间距规范
- 响应式设计优先

### 3. 组件结构

```vue
<template>
  <!-- 模板 -->
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'

// Props
defineProps<{
  prop1: string
}>()

// Emits
const emit = defineEmits<{
  event1: []
}>()

// State
const state = ref('')

// Computed
const computedValue = computed(() => state.value)

// Methods
const handleClick = () => {}

// Lifecycle
onMounted(() => {})
</script>

<style scoped>
/* 仅在必要时使用 */
</style>
```

---

## 📝 代码风格

### 1. 导入顺序

```typescript
// 1. Vue 核心
import { ref, computed } from 'vue'

// 2. 第三方库
import axios from 'axios'

// 3. 项目别名导入（使用 code/*, module/* 等）
import { success, error } from 'code/Utils/response'
import { Project } from 'code/Models/project'

// 4. 相对导入（尽量避免）
// import { helper } from './helper'  // 避免
```

### 2. 命名规范

```typescript
// 文件名：kebab-case
// user-service.ts, project-list.vue

// 组件名：PascalCase
// UserProfile.vue, ProjectList.vue

// 函数名：camelCase
function getUserInfo() {}

// 常量：UPPER_SNAKE_CASE
const API_BASE_URL = 'https://api.example.com'

// 类型/接口：PascalCase
interface UserInfo {}
type ProjectStatus = 'active' | 'archived'
```

### 3. TypeScript

```typescript
// ✅ 推荐：明确类型
function createUser(data: { name: string; email: string }): number {
  // ...
}

// ✅ 推荐：处理 catch 错误类型
try {
  // ...
} catch (err) {
  const message = err instanceof Error ? err.message : 'Unknown error'
}

// ❌ 避免：使用 any
function getData(params: any): any {  // 不推荐
  // ...
}
```

---

## 🔒 安全规范

### 1. SQL 注入防护

```typescript
// ❌ 危险：SQL 注入风险
const sql = `SELECT * FROM users WHERE id = ${userId}`

// ✅ 安全：使用命名参数
k.DB.sqlite.get('SELECT * FROM users WHERE id = @userId', { userId })
```

### 2. 鉴权检查

```typescript
// 所有需要登录的 API 都必须检查
if (!k.account.isLogin) {
  return error('Unauthorized', 401)
}
```

### 3. 权限检查

```typescript
// 敏感操作必须检查权限
if (!checkProjectPermission(projectId, currentUser.id, 'admin')) {
  return error('Forbidden', 403)
}
```

---

## 📖 文档和注释

### 1. 函数注释

```typescript
/**
 * 创建项目
 * @param data 项目数据
 * @param creatorId 创建者 ID
 * @returns 项目 ID
 */
export function createProject(
  data: { name: string; description?: string },
  creatorId: number
): number {
  // 实现...
}
```

### 2. 复杂逻辑注释

```typescript
// 1. 检查用户权限
if (!checkPermission()) {
  return error('Forbidden', 403)
}

// 2. 验证数据完整性
const isValid = validateData(data)

// 3. 创建资源并关联关系
const resourceId = createResource(data)
createRelation(resourceId, userId)
```

---

## 🧪 测试和调试

### 1. 错误日志

```typescript
// 关键错误必须记录日志
catch (err) {
  k.logger.error('CategoryName', errorMessage)
  return error('Failed', 500, err)
}
```

### 2. 开发模式

- 使用 `npm run dev` 启动开发服务器
- 后端修改需要重启 Kooboo
- 前端支持热更新

---

## ✅ 提交前检查清单

开发完成后，请确保：

- [ ] 遵循 @k-url /api/resource/{action} 路由格式
- [ ] 使用 success 和 error 统一响应
- [ ] ID 通过 query 或 body 传递，不使用路径参数
- [ ] 所有 API 都有鉴权和权限检查
- [ ] 使用 ORM 操作数据库，避免手写 SQL
- [ ] 参数验证完整
- [ ] 错误处理健全
- [ ] TypeScript 类型正确
- [ ] 代码格式规范
- [ ] 添加必要的注释

---

## 📚 参考文档

- [后端开发规范](docs/Backend-Development-Guide.md) - 完整的后端 API 开发指南
- [K_SQLITE_ORM 文档](kscript-docs/K_SQLITE_ORM_文档.md) - ORM 使用手册
- [API 规范 v2](docs/API-rules-v2.md) - API 开发规范
- [API 示例 v2](docs/API-examples-v2.md) - API 示例代码

---

## 🎯 开发原则

1. **简洁优先**：代码简洁明了，避免过度设计
2. **安全第一**：所有输入必须验证，所有 SQL 使用参数化
3. **统一规范**：遵循项目统一的代码风格和 API 设计
4. **文档完善**：关键逻辑必须有注释说明
5. **测试充分**：考虑边界情况和异常处理

---

**注意：** 这份规范是强制性的，所有团队成员（包括 AI）在开发时都必须严格遵守。
